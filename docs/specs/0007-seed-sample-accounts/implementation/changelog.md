# Implementation Changelog — 0007 Seed Sample User Accounts per Role

What actually shipped, checkpoint by checkpoint. Append-only. This is the record `/validate`
and future specs consult to learn what is really in the code, as opposed to what was designed.

---

## CP-1 — Data layer: constants, seeding, migration · 2026-08-14

**Tasks completed:** T-01, T-02, T-03, T-04

**Files created**

| Path | Purpose |
|---|---|
| `backend/src/Db/Migrations/20260813210515_AddSeedSampleAccounts.cs` | Generated migration: `InsertData` for 3 `AspNetUsers` rows + 3 `AspNetUserRoles` rows; `Down()` deletes them by PK |
| `backend/src/Db/Migrations/20260813210515_AddSeedSampleAccounts.Designer.cs` | Generated migration model snapshot for this migration |
| `backend/tests/Ats.IntegrationTests/Auth/SeedAccountsMigrationTests.cs` | AC-1, AC-6, AC-7, AC-8, NFR-1, E-4 |

**Files modified**

| Path | Change |
|---|---|
| `backend/src/Shared/Auth/AuthConstants.cs` | Added nested `SeedAccounts` static class: 3 fixed seeded-user GUIDs and security stamps, 3 seeded emails, `SharedPassword` = `Temp@123`, pinned `SharedPasswordHash` (real `PasswordHasher<ApplicationUser>` output, computed via a throwaway xUnit fact, pasted in, then deleted), fixed `SeededAtUtc` |
| `backend/src/Db/AppDbContext.cs` | Added `private static void SeedUsers(ModelBuilder builder)` (3 `ApplicationUser` `HasData` rows + 3 `IdentityUserRole<Guid>` `HasData` rows); called from `OnModelCreating` immediately after `SeedRoles(builder)`; added `using Microsoft.AspNetCore.Identity;` for `IdentityUserRole<Guid>` |
| `backend/src/Db/Migrations/AppDbContextModelSnapshot.cs` | Regenerated by `dotnet ef migrations add`; reflects the 6 new `HasData` rows |

**Decisions made during implementation**

| # | Decision | Why |
|---|---|---|
| I-1 | Generated `SharedPasswordHash` via a throwaway `[Fact]` in `Ats.UnitTests/Auth/TempHashGenerator.cs` that wrote the hasher output to a scratch file, then deleted the fact file entirely (not left as a comment or stub) | Matches LLD §10 step 0 exactly; the pinned value now baked into `AuthConstants.SeedAccounts.SharedPasswordHash` and the generated migration is `AQAAAAIAAYagAAAAEOtx0o7XBEX91ZfQpzpRzQRdjjNKgXHPuOpNiKax7WTqd/M0OzWUpco0hClbyO1bJQ==` |
| I-2 | Killed two stale `Ats.Api`/`Ats.AppHost` processes left running from a prior session before the first `dotnet build` | They held file locks on `bin/Debug` DLLs, causing `MSB3021`/`MSB3027` copy failures unrelated to this checkpoint's code |

**Deviations from the LLD**

| LLD section | Designed | Actual | Reason | LLD patched? |
|---|---|---|---|---|
| §11, `Migrate_WithProductionEnvironment_StillSeedsAllThreeAccounts` | `WebApplicationFactory<Program>` subclass calling `IWebHostBuilder.UseEnvironment("Production")` | Direct `Ats.Service.ServiceCollectionExtensions.AddSystemService`-wired `ServiceCollection`/`ServiceProvider` with a hand-rolled `Production` `IHostEnvironment`/`IWebHostEnvironment` registered, `AppDbContext` resolved from it and migrated | `UseEnvironment("Production")` on a `WebApplicationFactory<Program>` for this app (tried both as a `ConfigureWebHost` override and via `factory.WithWebHostBuilder(...)` on the already-working `CustomWebApplicationFactory`) crashed the entire out-of-process test host with `CRITICAL: Missing required configuration key 'ConnectionStrings:Default'` — `Program.cs`'s own early `Environment.Exit(1)` guard (lines 14-20) runs against `builder.Configuration` before the test's `ConfigureAppConfiguration` override becomes visible, specifically when the environment differs from the test host's default. This is a pre-existing `Program.cs`/ASP.NET Core minimal-hosting interaction, unrelated to this spec's design, and out of this checkpoint's scope to fix by changing `Program.cs`. The DI-container workaround exercises the identical `AddSystemService` wiring `Program.cs` uses for `AppDbContext` without depending on that bootstrap timing. | Yes — `plan/lld.md` §11 test-plan row and Deviation Log updated |

**Verification run**

```
$ cd backend && dotnet build
Build succeeded.
    0 Warning(s)
    0 Error(s)

$ dotnet ef database update --project src/Db --connection "Data Source=/tmp/ats_final_verify.db"
Applying migration '20260805133328_InitialCreate'.
Applying migration '20260805141657_AddAuthenticationAndRefreshTokens'.
Applying migration '20260805171525_AddRequisitionsAndStages'.
Applying migration '20260805191845_AddApplicationsAndCvAttachments'.
Applying migration '20260806080934_AddPipelineProgression'.
Applying migration '20260813210515_AddSeedSampleAccounts'.
Done.

$ dotnet test tests/Ats.IntegrationTests --filter "FullyQualifiedName~SeedAccountsMigrationTests"
Passed!  - Failed: 0, Passed: 4, Skipped: 0, Total: 4, Duration: 1 s - Ats.IntegrationTests.dll (net10.0)

$ dotnet test tests/Ats.IntegrationTests
Passed!  - Failed: 0, Passed: 109, Skipped: 0, Total: 109, Duration: 16 s - Ats.IntegrationTests.dll (net10.0)

$ dotnet test tests/Ats.UnitTests
Passed!  - Failed: 0, Passed: 161, Skipped: 0, Total: 161, Duration: 2 s - Ats.UnitTests.dll (net10.0)
```

Fresh-database row counts verified directly against the applied SQLite file: `AspNetUsers` = 3,
`AspNetUserRoles` = 3, emails = `sample.candidate@d4fape-ats.local`,
`sample.recruiter@d4fape-ats.local`, `sample.hiringmanager@d4fape-ats.local`.

**Meta updates applied**

- `architecture.md`: no structural change to the component map or data model (no new
  component, no new entity — `User`/`Role` already exist) — Change Log row appended only.
- `tech-stack.md`: no change.
- `coding-standards.md`: no change.

**Known gaps carried into the next checkpoint**

- Full-suite regression pass and `meta/architecture.md`'s final confirmation are CP-3 (T-08, T-09).

---

## CP-2 — Behavioural verification: login, hash format, duplicate email · 2026-08-14

**Tasks completed:** T-05, T-06, T-07

**Files created**

| Path | Purpose |
|---|---|
| `backend/tests/Ats.IntegrationTests/Auth/SeedAccountsLoginTests.cs` | AC-3, AC-4, AC-5, E-1: Integration tests for logging in with each seeded account (Candidate, Recruiter, Hiring Manager) and verifying 409 duplicate email on registration |
| `backend/tests/Ats.UnitTests/Auth/SeedAccountsPasswordHashTests.cs` | AC-2: Unit test verifying seeded PasswordHash verifies against SharedPassword using PasswordHasher |

**Files modified**

| Path | Change |
|---|---|
| None | Pure addition of test files |

**Decisions made during implementation**

| # | Decision | Why |
|---|---|---|
| I-3 | Placed candidate, recruiter, and hiring manager login tests alongside duplicate registration test in `SeedAccountsLoginTests.cs` | Matches LLD §1 file manifest grouping |

**Deviations from the LLD**

| LLD section | Designed | Actual | Reason | LLD patched? |
|---|---|---|---|---|
| None | N/A | N/A | Implementation followed LLD design exactly | N/A |

**Verification run**

```
$ dotnet test tests/Ats.UnitTests
Passed!  - Failed: 0, Passed: 162, Skipped: 0, Total: 162, Duration: 2 s - Ats.UnitTests.dll (net10.0)

$ dotnet test tests/Ats.IntegrationTests
Passed!  - Failed: 0, Passed: 113, Skipped: 0, Total: 113, Duration: 17 s - Ats.IntegrationTests.dll (net10.0)

$ dotnet build; dotnet test tests/Ats.ArchitectureTests --no-build
Build succeeded. 0 Warning(s), 0 Error(s).
Passed!  - Failed: 0, Passed: 4, Skipped: 0, Total: 4, Duration: 66 ms - Ats.ArchitectureTests.dll (net10.0)
```

**Meta updates applied**

- `architecture.md`: no change.
- `tech-stack.md`: no change.
- `coding-standards.md`: no change.

**Known gaps carried into the next checkpoint**

- None — CP-3 is the final checkpoint.

---

## CP-3 — Hardening · 2026-08-14

**Tasks completed:** T-08, T-09

**Files created**

| Path | Purpose |
|---|---|
| None | Verification and meta documentation only |

**Files modified**

| Path | Change |
|---|---|
| `docs/specs/meta/architecture.md` | Updated date to 2026-08-14; appended 0007 CP-2 and CP-3 entries to Change Log, closing out spec |
| `docs/specs/0007-seed-sample-accounts/plan/tasks.md` | Marked T-08 and T-09 complete; all 9 tasks done |
| `docs/specs/0007-seed-sample-accounts/spec.md` | Status updated from `implementing` to `implemented` |
| `docs/specs/index.md` | Spec 0007 status updated from `implementing` to `implemented` |

**Decisions made during implementation**

| # | Decision | Why |
|---|---|---|
| None | N/A | Hardening and verification tasks only |

**Deviations from the LLD**

| LLD section | Designed | Actual | Reason | LLD patched? |
|---|---|---|---|---|
| None | N/A | N/A | No deviations | N/A |

**Verification run**

```
$ cd backend && dotnet build
Build succeeded.
    0 Warning(s)
    0 Error(s)

Time Elapsed 00:00:11.13

$ dotnet test tests/Ats.ArchitectureTests --no-build
Passed!  - Failed:     0, Passed:     4, Skipped:     0, Total:     4, Duration: 96 ms - Ats.ArchitectureTests.dll (net10.0)

$ dotnet test tests/Ats.UnitTests
Passed!  - Failed:     0, Passed:   162, Skipped:     0, Total:   162, Duration: 1 s - Ats.UnitTests.dll (net10.0)

$ dotnet test tests/Ats.IntegrationTests
Passed!  - Failed:     0, Passed:   113, Skipped:     0, Total:   113, Duration: 18 s - Ats.IntegrationTests.dll (net10.0)

$ cd ../frontend && npm test
Test Files  15 passed (15)
      Tests  59 passed (59)
   Duration  3.43s

$ npm run build
✓ Compiled successfully
✓ Generating static pages (9/9)

$ npm run lint
✔ No ESLint warnings or errors
```

All 162 unit tests, 113 integration tests, 4 architecture tests, and 59 frontend unit tests pass cleanly. Pre-existing tests (`AuthEndpointsTests`, `PipelineMigrationBackfillTests`) pass with no regressions from the newly seeded rows.

**Meta updates applied**

- `architecture.md`: Change Log appended for 0007 CP-2 and CP-3, closing out spec.
- `tech-stack.md`: no change.
- `coding-standards.md`: no change.

**Known gaps carried into the next checkpoint**

- None — all 9 tasks complete across all 3 checkpoints. Spec ready for `/validate`.

---
